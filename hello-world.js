// sahil gupta

/*
why acorn over esprima
both parse javascript and get the job done
both produce well-documented, beautiful ASTs
both have optional 'loose mode' where tolerates errors

but by esprima's own benchmarks (speed-test.png)
    acorn is midly faster
acorn half the lines of code


*/

console.log('hi');
